# HTML 铬浓度训练/预测工具

## Summary
- 做一个本地 Web 工具：用户打开一个 `index.html` 页面，页面有两个模式：`训练` 和 `预测`。
- 后端用 Python 提供训练、预测、模型保存能力；HTML 负责上传、进度、结果表和质控图展示。
- 训练模式按用户选择：点击上传一批图片，输入这一批对应浓度，可重复添加多组浓度。
- 训练时自动检查异常/偏差过大样本，拒绝进入训练，并在页面报告原因。

## Key Changes
- 前端页面：
  - `训练`：上传图片、输入浓度、添加训练组、开始训练、显示被拒绝样本和最终 MAE/RMSE。
  - `预测`：上传未知浓度照片，输出连续浓度、最近标准浓度、YOLO 圆斑框、质控状态。
  - 页面展示训练后的可用浓度范围、模型版本、训练样本数和异常样本数。
- 后端接口：
  - `GET /` 返回单个 HTML 页面。
  - `POST /api/train` 接收多组 `{浓度, 图片列表}`，训练 YOLO 圆斑定位和浓度回归模型。
  - `POST /api/predict` 接收未知照片，返回预测结果和质控 overlay 图。
  - `GET /api/model/status` 返回当前是否已有可用模型。
- 依赖：
  - 需要安装 `ultralytics` 用于 YOLO。
  - 使用已有 `cv2/sklearn/torch/joblib` 完成图像处理、回归训练和模型保存。

## Training Pipeline
- YOLO 圆斑定位：
  - 对训练图自动生成初始圆斑标注，训练单类别 `spot` 检测器。
  - 检出失败、框偏离中心过大、框尺寸异常的图片直接拒绝。
- 背景校正：
  - YOLO 检出圆斑后取内切圆 mask 作为显色区域。
  - 用圆斑外环/图像边缘估计背景，做白平衡和背景扣除。
- 浓度模型：
  - 提取 RGB/Lab/HSV、背景差值、颜色比值、吸光度近似特征。
  - 训练连续回归模型，优先使用小样本稳健模型：`ExtraTrees + SVR/PLS` 对比择优。
  - 保存 YOLO 权重、回归模型、特征处理器和训练配置。

## 偏差拒绝规则
- 单张图片拒绝条件：
  - YOLO 未检出圆斑或检出置信度过低。
  - ROI 明显偏离中心、过大/过小、过曝、过暗、严重模糊。
  - 同浓度组内颜色特征偏离中位数超过 robust MAD 阈值。
  - 初始交叉验证残差超过 `max(0.08 mg/L, 标称浓度的15%)`。
- 浓度组拒绝条件：
  - 某一浓度最终可用图片少于 3 张。
  - 某组超过 40% 图片被拒绝，页面提示该组需要重拍或重新上传。
- 被拒绝样本不进入最终训练，页面显示缩略图、输入浓度、拒绝原因。

## Test Plan
- 用当前 180 张标准图做端到端验证。
- 报告 5 折分层交叉验证 MAE/RMSE/R2，主目标 MAE `≤0.05 mg/L`。
- 报告最近标准浓度档位准确率和每个浓度点的误差。
- 手动检查训练/预测页面能上传图片、显示 ROI overlay、保存模型并重新预测。
- 对异常图片做测试：偏移、模糊、曝光异常、错误浓度标签，确认会被拒绝或标记质控失败。

## Assumptions
- HTML 是用户入口，但训练和预测由本地 Python 后端执行。
- 用户每次训练可以多次添加“图片批次 + 一个浓度值”。
- 模型只保证在训练浓度覆盖范围内预测；超出范围时页面标记为外推风险。
